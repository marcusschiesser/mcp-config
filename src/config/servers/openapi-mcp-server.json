{
  "name": "openapi-mcp-server",
  "url": "https://github.com/snaggle-ai/openapi-mcp-server",
  "description": "Connect any HTTP/REST API server using an Open API spec (v3)",
  "command": "npx",
  "args": {
    "fixed": ["-y", "openapi-mcp-server"],
    "configurable": [
      {
        "type": "named",
        "name": "spec",
        "flag": "--spec",
        "description": "Path to OpenAPI specification file",
        "required": true
      }
    ]
  },
  "env": [
    {
      "name": "API_KEY",
      "description": "API key for accessing the OpenAPI service (if required)"
    }
  ]
}
